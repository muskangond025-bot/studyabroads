Redesign an existing “How It Works” section for an Event Management website to make it visually rich, timeline-driven, and animation-ready.

Frame & Base:

Desktop frame: 1440px

Section width: 1100px centered

Padding: 120px top & bottom

Background: soft gradient (top #F8FAFC → bottom #EEF2FF)

Header:

Title: “How It Works”

40px, bold, center

Subtitle:

“From planning to execution, we simplify every step”

16px, muted gray

Add subtle decorative blur shapes behind header (light purple/blue, 10% opacity)

Advanced Timeline Design:

Replace simple line with:

Gradient vertical line (2px, #6366F1 → #A78BFA)

Add glow effect (outer blur 8px, low opacity)

Timeline nodes:

Size: 20px outer + 10px inner dot

Outer ring: gradient stroke

Inner fill: primary color

Add soft pulse effect (for animation states)

Step Layout (Premium Cards):

Alternate left-right layout

Each step connected with a curved connector line (not straight)

Card Design (IMPORTANT – make it premium):

Width: 440px

Padding: 24px

Background: glassmorphism style:

rgba(255,255,255,0.7)

backdrop blur: 12px

Border: 1px solid rgba(255,255,255,0.4)

Radius: 16px

Shadow: 0 10px 30px rgba(0,0,0,0.08)

Inside Card:

Small icon (top-left, inside soft colored circle)

Step label: “Step 01” (12px, uppercase, letter spacing)

Title: 20px semi-bold

Description: 14px, 2 lines max

Add small arrow or progress indicator

Depth & Layering (Make it NOT flat):

Slight vertical offset for alternating cards (zig-zag depth)

Add floating shadow under cards

Add background abstract shapes (blurred blobs behind timeline)

Animation Preparation (VERY IMPORTANT):
Design components in a way that supports Figma Smart Animate:

Create 2 states for each step:

Default state:

Card slightly faded (opacity 70%)

Scale: 0.96

Active state:

Card full opacity

Scale: 1

Node glows

Connector line highlights (stronger gradient)

Add hover interaction:

Card lifts (translateY -6px)

Shadow increases

Icon slightly scales (1.05)

Micro-interactions:

Timeline line fills from top to bottom (progress feel)

Nodes appear one by one (staggered spacing design)

Use consistent spacing: 100px between steps

Typography & Colors:

Font: Inter

Primary: #6366F1

Accent gradient: #6366F1 → #A78BFA

Text: #0F172A / #64748B

Mobile Behavior:

Timeline shifts left

Cards stack right

Remove alternating layout

Keep animation states intact

Do:

Make it visually layered, not flat

Use gradients, blur, and soft glow

Maintain clean spacing despite richness

Ensure animation-ready structure (Auto Layout + components)

Don’t:

Avoid plain straight lines and basic dots

Don’t use heavy dark UI

Avoid overcrowding cards

Don’t break alignment of timeline